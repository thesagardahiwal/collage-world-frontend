/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com'], // add any domains from which you are loading images
      },
};

export default nextConfig;
